#!/usr/bin/env python
#project week 13
import os


directory = os.getcwd()
file_attr_list = []

for file_name in os.liststat(directory):
    
    for file_size in range(os.path.getsize(directory)):
        
        file_attr = {
        "name": os.liststat(file_name)
        "size": range(os.path.getsize,
            
        }

file_attr_list.append(file_attr)

print(file_attr)
